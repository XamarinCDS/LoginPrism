<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="LoginPrism.Views.LoguedPage">
    <ContentPage.Content>
        <StackLayout>
            <Button Text="Docentes" BackgroundColor="Coral" TextColor="White" Command="{Binding DCommand}"/>
            <Button Text="Materias" BackgroundColor="MediumSeaGreen" TextColor="White" Command="{Binding MCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>